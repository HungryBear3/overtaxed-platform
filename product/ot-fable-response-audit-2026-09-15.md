# Fable response audit — 2026-09-15

## Integrity

The attached response archive contained 10 flat regular files. `unzip -t` passed and every entry matched `MANIFEST.sha256`.

## Accepted

- Neutral $69 records-and-matching-property report.
- Four-state pre-payment gate based on scope, report-field completeness, and a same-day official open Assessor window.
- Bounded QA-only review and full refund when the promised report cannot be completed.
- Manual evidence ledger for the first cohort while the automated T2 chain remains inactive.
- Strict automated qualification remains separate and unsigned.

## Corrections required before customer use

1. Replace “Comparable Property Report” with “Matching Property Report” on offer surfaces.
2. Replace every “0 most likely means not recorded” statement with: “The county’s published transformation does not distinguish a raw missing value from zero; OT therefore treats the published 0 as ambiguous and does not interpret it.”
3. Do not default a silent HOLD customer to delivery. If a customer response is required to resolve what was promised, no response results in refund; if the report is already complete and the issue is disclosure-only, no response is needed.
4. Do not deploy copy before the neutral artifact producer exists. The legacy generator drafts an appeal argument and requested value, while the current T2 producer is tied to the unsigned strict-eligibility threshold.
5. Do not promise an embedded source image until Cook County image-reproduction permission is resolved. Use attributed text/data and source links meanwhile.

## Reconciled technical finding

Current code proves that a global checkout shutdown is not the desired business rule, but it also proves that “copy-only” is insufficient. The checkout snapshot and settlement path currently depend on the signed strict-eligibility policy, and production artifact generation refuses when that policy is unsigned. A safe implementation must introduce a separately versioned neutral-report commerce policy and remove the merits-gap threshold from the neutral artifact without altering the strict research model.
